<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Annie Jean Photography</title>
  <link rel="stylesheet" href="/public/styles.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" type="image/jpeg" href="/public/logo.jpg">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="max-w-lg mx-auto p-8 bg-white rounded-lg shadow-lg text-center">
    <div class="flex justify-center mb-6">
      <img src="/public/logo.jpg" alt="Annie Jean Photography Logo" class="h-20 object-contain" onerror="this.onerror=null;this.src='/public/logo.jpg';">
    </div>
    
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-gray-800 mb-4">Oops! Something went wrong</h1>
      <div class="bg-red-100 text-red-800 px-4 py-3 rounded border border-red-200 mb-4">
        <%= message %>
      </div>
    </div>
    
    <div class="space-y-3">
      <button onclick="history.back()" class="btn-gold-gradient px-6 py-2 w-full">
        Go Back
      </button>
      
      <a href="/" class="btn-gold-outline px-6 py-2 w-full inline-block">
        Return Home
      </a>
      
      <details class="text-left">
        <summary class="cursor-pointer text-sm text-gray-600 hover:text-gray-800">
          Need help? Try these steps:
        </summary>
        <div class="mt-3 text-sm text-gray-600 space-y-2">
          <p><strong>For file upload issues:</strong></p>
          <ul class="list-disc list-inside space-y-1 ml-4">
            <li>Make sure your images are under 10MB each</li>
            <li>Try compressing your images using an online tool</li>
            <li>Check your internet connection</li>
            <li>Try uploading one file at a time</li>
          </ul>
          
          <p class="mt-4"><strong>For form submission issues:</strong></p>
          <ul class="list-disc list-inside space-y-1 ml-4">
            <li>Fill out all required fields</li>
            <li>Make sure your email address is valid</li>
            <li>Try refreshing the page and submitting again</li>
            <li>Disable any ad blockers temporarily</li>
          </ul>
          
          <p class="mt-4"><strong>Still having trouble?</strong></p>
          <p>Please contact us and mention error code: <code class="bg-gray-200 px-1 rounded"><%= new Date().getTime() %></code></p>
        </div>
      </details>
    </div>
    
    <% if (typeof error !== 'undefined' && error && process.env.NODE_ENV === 'development') { %>
    <details class="mt-6 text-left">
      <summary class="cursor-pointer text-sm text-red-600">Debug Info (Development)</summary>
      <pre class="mt-2 text-xs bg-gray-100 p-3 rounded overflow-auto"><%= JSON.stringify(error, null, 2) %></pre>
    </details>
    <% } %>
  </div>
</body>
</html> 