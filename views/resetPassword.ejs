<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Password</title>
  <link rel="stylesheet" href="/public/styles.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" type="image/jpeg" href="/public/logo.jpg">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="max-w-xl mx-auto p-12">
    <div class="flex justify-center mb-6">
      <img src="/public/logo.jpg" alt="Annie Jean Photography Logo" class="h-28 object-contain" loading="lazy" onerror="this.onerror=null;this.src='/public/logo.jpg';">
    </div>
    <h1 class="text-2xl font-bold mb-6 text-center">Reset Password</h1>
    <% if (error) { %>
      <div class="bg-red-100 text-red-800 px-4 py-2 rounded mb-4 text-center"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="bg-green-100 text-green-800 px-4 py-2 rounded mb-4 text-center"><%= success %></div>
    <% } %>
    <form action="/reset-password/<%= token %>" method="POST" class="space-y-6">
      <div>
        <label for="password" class="block text-sm font-medium text-gray-700">New Password</label>
        <input type="password" id="password" name="password" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-400 focus:ring-green-400 py-2 px-4">
      </div>
      <div>
        <label for="confirmPassword" class="block text-sm font-medium text-gray-700">Confirm New Password</label>
        <input type="password" id="confirmPassword" name="confirmPassword" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-400 focus:ring-green-400 py-2 px-4">
      </div>
      <button type="submit" class="btn-gold-gradient px-8 py-2 w-full">Reset Password</button>
    </form>
    <div class="text-center mt-4">
              <a href="/login" class="text-sm text-green-700 underline hover:text-green-900">Back to Login</a>
    </div>
  </div>
</body>
</html> 